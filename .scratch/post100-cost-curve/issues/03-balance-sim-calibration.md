# 03 - balance-sim 校准（临时脚本，跑完删）

**Status:** resolved
**Type:** task
**Blocked by:** 01, 02

## 任务

临时 vitest（`post100-balance-sim.tmp.test.ts`，跑完删除）验证 spec 校准约束：

1. **相对价格同阶**：NG+0/10/30/50（permanentMult 1/2.5/5.5/8.5，覆盖 ×64=30 周目、×1024=50 周目量级），
   count=200 时买入/升级秒产出的跨周目比值。
2. **有摩擦**：count=100→200 买入秒产出增长倍数。
3. **不死档**：count=200 买入 < 24h 产出、升级 < 30 天产出。
4. 输出分布表 → 判定 `POST100_GROWTH=1.05` / `POST100_BUY_TARGET_SECONDS=3` 是否需调参。

## 验收

- 校准报告：低/高 NG+ 相对价格对比 + 定稿值。

## Answer

**校准通过，常量保持初值**（分布见 spec.md 校准表）：

| count | 买入（秒产出） | 升级（秒产出） |
|---|---|---|
| 100 | 0.1–0.8s | 9.8–83s |
| 101 | 3.15s | 318s ≈ 5.3 分钟 |
| 150 | 34.4s | 86 分钟 |
| 200 | 394.5s ≈ 6.6 分钟 | 21.9 小时 |

- **跨周目比值 = 1.00**（买入与升级口径）——动态下限完美跟随产出，×64/×1024 不塌缩（spec 核心诉求达成）。
- 100→200 台买入 ×475–4040（低→高周目），摩擦显著。
- 200 台升级 21.9h < 30 天，不死档；买入 6.6 分钟可负担。
- 注：200 台升级 ≈ 21.9h 而非 spec 估算的 11h，因保结构公式 `升级 = 买入×count` 在 count=200 时
  mult=200 放大；仍在验收带内（有摩擦但不死档），且与「买/升 ROI≈P=2 不漂移」的决策一致。
