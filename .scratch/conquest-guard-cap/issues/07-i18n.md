# 07 — i18n：科技/成就文案 + ui.tech 错位核对

**What to build:** 新科技、新成就的中英文案；科技面板攻占门槛提示 key；顺带核对 `ui.tech` 数组既有索引错位（zh.ts 数组 3 项 vs render/tech.ts 消费到索引 2/3 的疑点）。

**Blocked by:** 02, 05, 06

**Status:** done

- [x] `zh.ts` + `en.ts` `tech.conquestTheory`：name「劫掠战术」/ desc（含 pct/pct2/n 占位：产出每级 +10%、消耗每级 −5%、需已攻占 5 个目标）
- [x] `zh.ts` + `en.ts` `ach.conquests10/25/50`：name/desc（descArgs n）
- [x] `zh.ts` + `en.ts` `ui.tech` 数组追加攻占门槛提示 key（供 render/tech.ts 锁定卡消费；若追加使索引后移，同步更新 render/tech.ts 对应 `t('ui.tech.N')` 消费点）
- [x] ⚠️ 核对修复：`zh.ts` L607-611 `ui.tech` 数组当前仅 3 项，而 `render/tech.ts` 使用 `ui.tech.0/1/2/3`——确认各消费点实际取到的文案（`ui.tech.1` 在结盟门槛分支被当"需结盟 N"用，但数组 [1] 是 '✓ 生效中'；`ui.tech.3` 越界）。顺带修正消费索引或数组内容，**不动既有文案语义**（改动需在 PR 说明）
- [x] `en.ts` 与 `zh.ts` key 完全同步（i18n 测试/类型检查覆盖：`DeepKey<Zh>` 保证）
- [x] 跑 i18n 相关测试（`src/i18n/index.test.ts` 或类型检查）确认无 key 缺失
