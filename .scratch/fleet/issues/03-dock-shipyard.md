# 03 — 船坞 + 造舰（垂直切片：可建可造可管理）

**What to build:** 第一个可演示切片——玩家能完整走通"建船坞 → 升船坞 → 造护卫舰 → 看到舰队规模"。建造/升级为硬约束（付不起不可点），舰数受船坞等级上限约束，购买成本逐艘递增，每次购买有明确反馈（日志/资源扣减）。舰队此时尚无防御效果（下一切片接线），但玩家已能看到能源支出入口与维护费预览。

**Blocked by:** 01 — 数据模型 prefactor；02 — 存档 v7 → v8

**Status:** resolved

- [x] 购买护卫舰 action：扣矿物+能源（第 n 艘成本 `base × 1.5^(n-1)`），资源不足返回失败提示（硬约束，与派遣/威慑同语义）；到达船坞等级上限后不可购买（提示上限）
- [x] `fleet.count` 随购买递增并持久化（保存/读档往返一致）；NG+ 重置为 0
- [x] 星域页建筑面板新增「舰队」分组（interstellar 分组内船坞卡 + 舰队管理区）：当前舰数 X/Y、建造按钮（含第 n 艘成本预览）、总维护费与战力预览（`-X 能源/s`）；全部 `data-fleet-*` 语义化；船坞 0 级时显示锁定原因（星港前置）
- [x] 船坞升级交互：unique 公式成本、Lv3 封顶后升级按钮置灰/隐藏
- [x] 引擎单测：造舰成本曲线（n=1..上限）、硬约束（资源不足拒绝且状态不变）、上限拦截、船坞升级封顶
- [x] dom 冒烟：舰队分组渲染（舰数/成本/维护费预览/锁定原因 `data-fleet-*`）；全量 vitest 回归绿 + typecheck clean
