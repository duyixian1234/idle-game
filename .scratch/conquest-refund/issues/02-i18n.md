## Parent

https://github.com/duyixian1234/idle-game/issues/14

## What to build

攻占军力返还文案的双语言 i18n 键（`src/i18n/zh.ts` + `src/i18n/en.ts`）：`cq` 数组追加索引 12（对齐现有 0-11，在 `cq.11` boss 文案之后追加，不重排既有键）：

- zh：`'返还军力 {a0}'`
- en：`'Recovered {a0} military'`

该键被引擎结算日志（`cq.3` 捷报文案的 rewards 数组）引用，展示返还的军力数值。

## Acceptance criteria

- [ ] `zh.ts` 与 `en.ts` 的 `cq` 数组均在索引 12 追加返还文案，键序不重排
- [ ] 文案经 `i18n` 类型约束（`DeepKey<Zh>`）编译通过
- [ ] `src/i18n/index.test.ts`（若有键完整性断言）通过

## Blocked by

None — can start immediately.

## Status

ready-for-agent
