# 04 — 锁定卡折叠（每区上限 3 张 + 展开态内存）

**What to build:** 锁定卡引导与噪声的平衡——普通建筑区与星际工程区各自独立计算：锁定卡 ≤3 张全部展示；>3 张只展示前 3 张 + 折叠行「还有 N 项未解锁」（`data-locked-collapse`），点击折叠行展开该区全部锁定卡、再次点击收起。折叠展开态存 main.ts 内存变量（UI 会话状态，不进存档、250ms 重建不重置、刷新回默认收起，与 activePanelTab 先例同构）。军事 tab 仅 2 个军事建筑不参与折叠。锁定卡展开/收起不改变任何购买行为。

**Blocked by:** 02 — 卡片组件 + 响应式网格

**Status: resolved

- [ ] 每区独立折叠判定（普通建筑区/星际工程区；>3 张才折叠），折叠行 `data-locked-collapse`
- [ ] 展开态内存变量（main.ts，刷新回默认收起），折叠行点击展开/收起
- [ ] dom 冒烟：≤3 全显、>3 折叠行 + 展开后全显、收起再折叠；全量 vitest 回归绿 + typecheck clean
