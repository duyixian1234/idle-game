# idle-game（太空采矿挂机）

纯前端文本挂机游戏：建造建筑产出矿物/能源/科技点，研发科技，探索星球，外交 4 派系达成「星系统一联邦」结局，通关后开放无限模式与 NG+。单上下文仓库，域决策见 `docs/adr/`。

## Language

**周目（NG+）**:
一轮完整的「开局 → 通关」进程；通关后可开启新周目（`ngPlusLevel`），部分资产继承、资源与建筑重置。
_Avoid_: 二周目专属语义（周目是通用概念）

**图鉴（Codex）**:
跨周目永久保留的收藏记录——派系图鉴（`factionCodex`）与成就图鉴（`achievements`）；图鉴条目在新周目有初始好感加成/永久解锁语义，与「周目内进度」严格区分。
_Avoid_: 收藏、已解锁列表（语义太宽）

**星域 / 档案 / 探索 / 设置**:
一级导航的四个页面：星域（玩法主体：机制条 + 日志 + 建造/科技/外交/军事二级 tab）、档案（声望 + 成就图鉴 + 周目统计）、探索（终局派遣玩法，通关前为锁定占位页）、设置（音频/日志/存档/危险区/关于）。

**深模块（Deep Module）**:
引擎与 UI 的模块组织模式——每个域一个文件、只暴露窄接口、依赖方向收敛（域 → core/balance），拒绝全能 hub。
_Avoid_: 工具类、util（深模块是域内聚，不是通用函数桶）

**会话 UI 状态（Session UI State）**:
跨 250ms 全量重建记忆、但不进存档的渲染态（折叠展开/角标快照/弹窗开关/typewriter 进度/升级高亮等），集中在 `ui/session` 的 `SessionUiState`。
_Avoid_: 缓存（会话态是渲染事实，不是性能优化）

**tick/渲染同源（Tick-Render Single Source）**:
`ui/session` 的 `tickAndRender(nowMs)` 在同一闭包内先 `tick` 后 `render`，二者共享同一 `GameState` 引用——重操作（`setState` 替换引用）后 tick 推进与渲染展示不可能脱节。
_Avoid_: 主循环持有独立 state 变量（ADR-0043 修复的数值冻结根因）

**分区隐藏抽屉（Zone-scoped Hidden Drawer）**:
建造页三区（民用/星际工程/军事）各自的「已隐藏 (N)」抽屉展开态按 `zoneId` 独立存于会话态（`hiddenBuildingsOpen: Record<string, boolean>`），一处展开不影响他区。
_Avoid_: 全局布尔（军事区漏传即「点了没反应」的成因）

**重操作（Heavy Operation）**:
替换 `GameState` 引用的业务动作（导入/导出/重置/NG+ 序列），与 tick 原地修改是两种状态语义，走 `setState(next)` + 重建。
_Avoid_: 重置（重置只是重操作之一）

**根因子（Root Factor）**:
`balance.ts` 中共享数学族的根参数（等级加成/升级溢价/科技增长），调参只动根因子即传导全系统。
_Avoid_: 常量、魔法数（根因子是「有数学家族的参数」，与内容数据相对）

**内容数据（Content Data）**:
`data.ts` 中手工调校、不做公式派生的内容（建筑 baseCost/produces/costGrowth、星球解锁阈值、攻占守卫）。
_Avoid_: 数值配置（内容数据是「刻意显式」的边界声明）

**分域随机（Domain Random）**:
按 `RngDomain`（event/conquest/explore/generate/duration）隔离计数器的确定性随机；**结果型随机**（事件类型/攻占成功率/探索结果）走持久化计数器，**装饰型随机**（文案/间隔抖动）走即时流。
_Avoid_: 随机数、PRNG（分域随机特指「档案绑定 + 域隔离」这套机制）

**出发时固化（Freeze on Departure）**:
探索派遣在出发时掷出并落盘结果（`ExpeditionResult`）与时长（`finishAt`），结算只入账不重抽——防 SL 的结构保证。
_Avoid_: 预生成（出发时固化是「结果与随机序列解耦」，预生成是静态内容）

**唯一大件（Unique Megastructure）**:
count 恒为 1、禁重复建造与 bulk 的建筑（星系间三件 + 终局三座），升级成本与产出对称 ×2/级。
_Avoid_: 超级建筑、稀有建筑（唯一大件是「对称增长 + count=1」的数学类，不是稀有度）

**虫洞（Wormhole）**:
终局工程第三轨（探索线延伸，ADR-0042/0047）：结盟派系 ≥10 解锁「虫洞理论」科技后可建造（unique 大件 Lv1-10）；等级驱动双线扩张——探索线：每级 +1 探索信道（与跃迁枢纽槽位并列，总上限 20）、探索能源每级 −5%（封顶 −50%，仅基础派遣费）、发现新目标权重每级 +10%（仅非 resource 分支）、程序生成目标上限每级 +1；**军力线（ADR-0047）**：军力容量每级 +10%（Lv10 ×2），与军械科技并列成为第二条等级放大轴。
_Avoid_: 跃迁枢纽升级（虫洞是独立建筑等级，非枢纽延续）

**终局工程（Endgame Engineering）**:
> 通关后开放的三条独立路线——星环冶炼场（建设流，全局产出 ×2^level）、跃迁枢纽（探索流，+3 槽/收获 ×4/离线 12h）与虫洞（探索线延伸 + 军力线，+10 槽至 20/能源 −50%/发现权重 ×2/生成上限 +10/军力容量 ×2），三座皆可建、效果叠加。
> _Avoid_: 终局抉择（旧互斥语义，已废弃）；双轨（旧名，现为三轨）

**胁迫外交（Coercion Diplomacy）**:
勒索 → 进贡条约 → 臣服的三级阶梯 + 三重赎罪退出通道；解锁条件 = 军力上限 ≥5000 ∨ 遭遇过 raid，跨周目永久。
_Avoid_: 勒索（勒索只是阶梯第一级）

**无尽生成目标（Generated Target）**:
无限模式程序生成/手写保底的军事、外交、天体目标；**程序生成零永久加成**（防无限叠加摧毁 balance），定义快照随档落盘。**已归档条目字段压缩（ADR-0058）**：conquest/faction 归档后压缩为 `{kind,id,name,batch}` 白名单（UI 折叠区只消费 name）；planet 全量保留（产出/机制管线依赖）。自动化事件审计（automationHistory）按 12h 窗口 + 保底 50 条清理——两项均为运行时幂等行为，非结构变更，实测存档 318KB→179KB（省 43.9%）。
_Avoid_: 随机任务、无尽内容（生成目标是「一次性奖励铁律」约束下的特定对象）

**买/升 ROI（Buy-vs-Upgrade ROI）**:
买入与升级每 +1/s 产出的成本比；曾恒等于 `UPGRADE_PREMIUM`（P=2）——升级「值得但略亏」，买/升交替决策。**2026-08-08 修订（ADR-0036）**：普通可多次购买建筑的升级已取消，此不变量仅余理论意义（unique 升级不含 count，ROI 另由对称增长 ×2/级定义）。
_Avoid_: 性价比（ROI≡P 曾是恒等式，普通升级取消后该恒等式失效）

**机制二分（Two-Track Mechanism）**:
所有建造/增益机制明确二分——可多次购买建筑（miner/solar/lab/refinery/deepDrill/barracks/militaryPort）只走「数量维度」（产出=单台固定×count，无升级）；unique 大件与科技只走「等级维度」（×2/级或×1.7^level，count 恒 1 或不含 count）。消除"数量×等级"耦合（原普通建筑升级成本 ×count 双重放大）导致的强制策略序（ADR-0036）。**2026-08-08 修订（ADR-0039）**：解锁侧同理——星际建筑的前置门槛须落在数量维度（`requiresCount`，如星港矿场需深层钻机 ≥6 台 = 原 Lv10 产出天花板 48/s 的等效数量），不得引用已废除的普通建筑升级满级（`requiresMaxLevel` 废弃）。**2026-08-11 修订（ADR-0049）**：产出类科技线纵向补全至对称结构——矿（行星钻探×1.5/纳米制造×2）、能（太阳能效率×1.5/聚变电池×2.5）、科（计算加速×1.5/神经网络×2.5）各两条；军械科技上限 5→10，与虫洞并列军力容量双等级轴。
_Avoid_: 双轨（终局工程已用「双轨」指两座皆可建，此处二分指机制形态分类）

**动态下限（Dynamic Floor）**:
post100 成本曲线中「成本 = 3 秒 × 当前净产出」的下限——成本锚定产出侧，高周目相对价格不塌缩。
_Avoid_: 成本保底（动态下限是「锚定净产出」的机制，不是固定最小值）

**软降级（Soft Downgrade）**:
舰队维护费在能源不足时不扣费、停摆（不负债、不惩罚），恢复供能自动重启。
_Avoid_: 停机、瘫痪（软降级是「可恢复的停摆」，不是故障态）

**护航远征（Escorted Expedition）**:
带护航的探索派遣——单艘费锚定 ESCORT_ENERGY_SECONDS 秒能源净产出（ADR-0044：10→1，等效舰数放大后费率收敛），返还锚定（基础成本 + 远征费），是「溢出能源 → 探索收益」的转换器；单次护航费 ≤ 当前能源 50%（余额兜底，防抽干生产停滞）。
_Avoid_: 舰队任务（护航远征是「返还锚定」的经济机制，不是任务类型）

**相对价格（Relative Price）**:
成本相对当前产出的攒取时间（`timeToSave`，瓶颈资源口径），显示为「≈N 秒/分钟/小时产出」——跨周目可比的决策口径。
_Avoid_: 价格、成本（相对价格是「秒产出」语义，绝对数值是另一回事）

**军力容量（Military Cap）**:
军力的存储上限，唯一有容量约束的资源——`⌊(100 + 200×军港×(1+0.5·lv)) × (1+永久+声望) × (1+0.1×军械科技Lv) × (1+0.1×虫洞等级) × (1+0.02×深空军备Lv)⌋`；满员时兵营产出截断，容量同时锚定攻占投入上限与探索派遣军力消耗。**2026-08-08 修订（ADR-0036）**：militaryPort 升级取消后 portLevel 恒 0，`(1+0.5·lv)` 项失效，军港回 200/座线性；容量结构变为「数量流（军港×200）× 乘数流（永久/声望/军械科技/虫洞）」。**2026-08-10 修订（ADR-0047）**：虫洞等级成为第二条等级放大轴（每级 +10%，Lv10 ×2，与 ADR-0042 探索线并列的军力线效果），叠加方式为与军械科技独立的乘法因子。**2026-08-11 修订（ADR-0049）**：军械科技等级上限 5→10，`(1+0.1×军械Lv)` Lv10 = ×2，与虫洞 Lv10 ×2 乘法叠加 = ×4。**2026-08-13 修订（ADR-0060）**：深空军备成为第三条（无限）乘数轴，`(1+0.02×Lv)` 无封顶（名义 maxLevel 100，实际点不满）；运兵船独立池见下条。
_Avoid_: 军力上限（军力容量是「数量流 × 乘数流」结构，不是简单上限）

**运兵船（Troop Transport）**:
boss 专用的独立军力池（ADR-0061）——军力自主容量即时存入/取出（存款语义、无费用），仅作 boss 出征支付源（池优先，池不足时主容量补），不参与 raid 防御/探索派遣/勒索臣服门槛（那些走主容量的驻防/威慑语义）。池容量 = `兵力上限 × (基础池 5% + C%) × (1 + 2%×无尽层数)`——兵力上限为基数（基础兵力越强池越大）、基础池 5% 保底（无攻占积累也有）、C 由攻占积累（静态 4 区各 +5%、boss 每层 +3%，周目内重置；生成目标不计，对齐 ADR-0012）、探索进度（无尽层数）每层 +2% 作用于整体。boss 守卫公式不动（锚主容量 cap、不含池）——池增长是纯收益、不推高守卫；boss 攻占成功返还 ⌊投入×50%⌋ 回池（残兵归库，ADR-0056 同族）。**2026-08-13 修订①**：+基础池 5% 与探索加成 2%/层（池容量 = cap×(5%+C%)×(1+2%×层数)）。**修订②**：boss 发起（手动/autoBoss）主容量支付**突破安全垫**（全量可付）——池已隔离 boss 消耗、主动发起是玩家决策；自动攻占普通目标仍保留安全垫保底（autoConquestTick 单独保证）；boss 目标不出现在军事攻占列表（无尽面板单入口），autoBoss 独立于 autoConquest 生效（只开 autoBoss 也自动打 boss）。
_Avoid_: 舰队（运兵船是军力存储通道，不是舰船载具）

**质变奖励（Qualitative Reward）**:
长升级线末端挂载的「摩擦降低」型效果（如 Lv10 派遣军力 −10%、Lv20 护航费 −10%），不改变收益倍率或产出上限，只为满级提供可感知的终点感。
_Avoid_: 满级奖励（质变奖励是「结构零扰动」的摩擦侧改动，收益侧奖励会破坏平衡）

**同源锚定（Co-source Anchoring）**:
生成目标的一次性经济（军事奖励/攻占成本/外交礼包）统一锚定当期净产出的数学族——成本与奖励同源缩放，净比值恒定，任何经济规模不漂移，结构性防印钞。与探索成本 `scaledClamp`、post100 动态下限、护航返还锚定同族。
_Avoid_: 产能挂钩（同源锚定强调「成本与奖励同源」的防印钞性质，不只是「奖励跟着产能走」）

**建交礼包（Discovery Gift）**:
生成外交对象（`endless:` / `gen:faction`）发现瞬间的产能挂钩一次性资源礼包（矿+科技双发）+ 好感 +10——好感恰好落在自动外交阈值 40 之下，零钳制逻辑。
_Avoid_: 见面礼（建交礼包是「产能锚定 + 阈值下好感」的数值结构，不是普通赠礼）

**自动外交模式（Auto Diplomacy Mode）**:
外交自动化纯全局方向（ADR-0032）：全局选 友好（自动贸易→结盟，仅 ended/infinite）/ 胁迫（生成派系自动勒索→条约）二选一；自动贸易好感阈值 0（发现礼包后新派系自动启动前置）；raid 安全边界（静态/探索派系在胁迫方向下自动跳过）；挂机（离线）同步推进。
_Avoid_: 逐派系配置（2026-08-08 迭代后为纯全局，perFaction 已废弃）

**自动攻占（Auto Conquest）**:
军事目标的自动攻占机制（ADR-0033）：独立开关、每 60s 对可用生成军事目标投满守卫必成发起、军力保底 10%（保留 raid 击退/探索派遣的军力）、挂机同步；守卫 = `min(max(500, 军力名义产能×40s), 军力上限×1/3, 名义产能×180s)`（ADR-0033 修订 + conquest-guard-cap 双上限：攻占所需兵力 ≤ 总兵力 1/3、≤ 3 分钟生产时间；上限优先，早期容量/3 < 500 时守卫 = 容量/3）——攻占门槛与玩家军事规模直接挂钩。目标选择按资源消耗升序优先（ADR-0052）：守卫（军力投入）升序为主序、快照资源费 costMineral+costEnergy 升序为平局打破——先易后难。**2026-08-12 修订（ADR-0056）**：攻占成功后返还 ⌊投入军力×50%⌋（受军力容量截断、失败全损、按 invested、排除 fleetLocked，手动/自动/静态/动态/boss 统一结算管线）——军力从"消耗品"转"半回收投资"，缓解后期探索生成军事目标速率 > 自动攻占处理速率的积压漏斗。**2026-08-12 修订（ADR-0057）**：同一冷却周期内军力充足时批量发起多个目标（守卫升序逐目标判定，军力不足 break，资源费不足 continue + 暂停重试）——吞吐从 1 目标/60s 提升至军力/守卫（主档 ≈3 目标/60s），军力保底逐目标判定保证 raid/探索安全垫恒存。
_Avoid_: 自动军事（自动攻占仅生成目标，静态主线区域手动）

**攻占科技（Raid Tactics / 劫掠战术）**:
攻占科技线（ADR-0051）：已攻占 ≥5 个目标解锁（全口径 conqueredCount，静态+动态全算）；每级攻占产出 +10%、攻占消耗 −5%（Lv10 满级：产出 ×2、消耗 ×0.5）；产出在结算时按当前等级实时乘（静态+动态全适用）、消耗在目标生成时固化快照（升级后新目标立享折扣）。
_Avoid_: 消耗影响军力投入/攻占时长（消耗口径仅经济费 costMineral/costEnergy；守卫已由 1/3 硬约束覆盖）

**攻占成就梯度**:
攻占数量收集成就（ADR-0051）：conquests2（≥2，rep 4）/ conquests10（≥10，rep 4）/ conquests25（≥25，rep 5）/ conquests50（≥50，rep 6）——周目内口径，NG+ 重置后可重解锁。

**派生折叠（Derived Archiving）**:
折叠判定由状态派生而非写死存档——`allied / subjugated / 条约中` → 折叠区，状态变化（到期/叛变/赎罪）自动折/展；折叠区保留胁迫管理入口（赎罪/续签）。
_Avoid_: 归档（派生折叠是「临时态自动回流」的生命周期语义，与结盟/攻占的永久归档不同）

**结算阶段组（Tick Group）**:
`tick()` 按结算阶段分组的 DAG 节点（资源链/外交链/事件链/结算链/结局链），组间显式依赖边（`after`）构成偏序、拓扑排序确定执行序，组内保持原线性调用（ADR-0034）。新 tick 域 = 新组声明依赖边，无需手插序列。
_Avoid_: tick 步骤（结算阶段组是「可声明的偏序节点」，不是单个调用步骤）

**黄金序（Golden Order）**:
注册表化重构前固化的旧执行序列快照——测试断言「注册拓扑序 == 黄金序」防顺序漂移；未来扩展（新 tick 组/新渲染节点）须显式更新快照，变更在 diff 中可见（ADR-0034/0035）。
_Avoid_: 快照测试（黄金序是「保序可回归资产」的语义，不是通用 snapshot 工具）

**渲染阶段（Render Phase）**:
render 注册表节点的阶段分类（`content` / `overlay` / `badge`）——overlay 强制末位表达 z-order 唯一真约束，其余阶段内顺序无关（ADR-0035）。状态副作用（logdir 按钮/tab disabled/滚动）不节点化，留 render() 主函数。
_Avoid_: 渲染优先级（渲染阶段是「z-order 结构约束」，不是性能优先级）

**成就刷新强调（Achievement Flash Emphasis）**:
新解锁成就的双轨视觉反馈——flash 动画（`ach-unlock-flash`，1.1s 琥珀色边框发光，一次性播放，1.2s 窗口过期不重放）+ 持续高亮（`NEW` 角标 + 边框发光，直到用户进入档案页查看后清除）。flash 由 UI 层 diff（`lastRenderedAchievementIds` 集合差集）驱动，持续高亮由 `seenAchievementMaxAt` 阈值判定（`unlockedAt > seenAchievementMaxAt`）。双轨原因：挂机场景下用户可能未注视屏幕，flash 即时反馈不够，持续高亮保证回来后仍可见。
_Avoid_: 成就通知（刷新强调是「渲染态反馈」，不是弹窗/toast 通知系统）

**日志筛选（Log Filter）**:
日志区按 `LogType`（system/story/event/reward/warning）单选筛选，DOM 层 CSS 属性选择器过滤（容器 `data-log-filter` 属性 + 行 `data-log-type` 属性，零 JS 遍历），筛选状态持久化到 `localStorage`。事件卡片（`.event-stack`）不受筛选影响（待办交互元素始终可见）。
_Avoid_: 日志搜索（筛选是「类别单选过滤」，不是全文搜索/时间范围查询）

**探索收获（Expedition Yield）**:
探索派遣结算时 resource 分支入账的 mineral/energy/tech 三元组（含护航返还补偿）——「探索天体处获得的资源」的唯一统计口径；记入探索统计字段（`exploreMineralEarned` 等）并并入全局累计。建交礼包（发现派系）与产出型天体的持续产出不属于探索收获（前者属发现语义，后者已由生产路径计入全局累计，避免双计）。ADR-0041。
_Avoid_: 天体奖励（探索收获特指「派遣结算的 resource 分支」，不含发现/持续产出路径）

**累计获得（Total Earned）**:
全局累计字段（`totalMineralEarned`/`totalTechEarned`/`totalEnergyEarned`）升级后的语义——代表「全部来源获得」（生产 + 产出型天体持续产出 + 探索收获），周目内口径、NG+ 重置。区别于「累计采集」旧语义（仅生产路径）；探索收获并入后展示文案用「累计获得」。ADR-0041。
_Avoid_: 累计采集（旧语义已升级，探索收获并入后不再是纯采集口径）

**可安装外壳（Installable Shell）**:
应用外壳（HTML/CSS/JS/字体/图标）经 Service Worker 预缓存形成的离线可加载 + 可安装形态（manifest + SW，ADR-0050）——**只管应用加载，不管离线收益**（离线收益是引擎对存档时间差的结算，二者正交互补、引擎零感知）。_Avoid_: 离线模式（离线收益是引擎语义，与外壳缓存正交）

**无尽成长轴（Endless Growth Axis）**:
层数作为跨 NG+ 继承的后期成长轴（ADR-0053）：层推进走平滑进度制（每次征服 +0.04、每次探索 +0.008，进度满 1.0 进位 1 层），每 3 层出现一次 boss 军力挑战（复用攻占管线），每层 +1% 全产出永久加成（与 NG+ 倍率叠乘、有叠加上限）。层数从 0 自然可达（修复原「仅 boss 击败且 boss 需 layer≥3」的死锁）。_Avoid_: 无限层数（成长轴有数值上限约束，防 runaway）

**护航同杠杆（Escort Same Lever）**:
护航远征的等效舰数 E 仅作费用侧吞吐杠杆（fee ∝ 能净产出 × E），不再贡献收获倍率（ADR-0054）——回报/投入比恒为常数且低于印钞阈值。_Avoid_: 舰队加成（E 的倍率贡献已移除，大舰队 = 更高吞吐、效率不变）

**无限科技（Infinite Tech）**:
三条可无限升级的后期科技线——产出线「深空冶金」+2%/级全产出、吞吐线「深空导航」+2%/级护航吞吐（ADR-0055），军力线「深空军备」+2%/级军力容量（ADR-0060）；成本统一 1e9 矿 + 2e8 科 ×1.7^n，maxLevel 名义 100（永远点不满），为存量资源提供永续出口。**ADR-0060 修订**：军力线刻意打破 ADR-0055「无限科技军力不吃」原始红线——军力是唯一有容量截断的资源，其天花板（军港成本爆炸 + 军械/虫洞双轴 Lv10 封顶）是后期瓶颈，需要「容量增长通道」而非产出 sink；放大容量不推高 boss 相对难度（守卫容量锚与 cap 同步缩放、比例恒定）。_Avoid_: 满级科技（无限科技点不满，是 sink 不是终点）
