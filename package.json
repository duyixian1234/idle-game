{
  "name": "idle-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "文本式太空采矿挂机游戏",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  }
}
